n8n Automation Projects — Muhammad Awais

A collection of AI-powered automation workflows built with n8n, connecting real APIs (OpenAI, Twilio, WhatsApp, Google Workspace, SerpAPI, Kie.ai/Veo) to solve real business problems — not demos, live working systems.

Contact: awaischskg456@gmail.com | 0309 9145478 | LinkedIn

Projects
🏥 AI Missed-Call & SMS Recovery Assistant

Every missed call at a clinic instantly triggers an automated text so the caller knows they've been heard. Genuine emergencies are escalated to staff immediately. An AI SMS agent with conversation memory handles the rest — answering questions, confirming bookings, and logging every appointment automatically. Stack: n8n, Twilio (SMS + Voice), OpenAI GPT, Conversation Memory, Structured Output

🔥 AI Lead Scoring & Automated Follow-Up Engine

New leads hit a webhook and are instantly normalized and scored by an AI model. Hot leads trigger real-time Slack and email alerts to the sales team, while an AI agent drafts and sends a personalized follow-up — zero human touch required. Stack: n8n, AI Lead Scoring, Slack API, Gmail API, Google Sheets

🎬 AI Video Generation Agent (Text-to-Video)

A form submission passes a brief through a two-stage AI agent chain that refines the prompt, then calls the Kie.ai / Google Veo video generation API — submits the job, polls status on a wait-loop, and retrieves the finished video automatically. Stack: n8n, Multi-Agent Chaining, Kie.ai / Veo API, Async Polling Loop

💬 Multi-Modal WhatsApp AI Business Assistant

A WhatsApp-native AI assistant that understands text, voice notes (auto-transcribed), and images (auto-analyzed). Every message is routed to an AI agent with memory and a live Google Search tool for real-time answers. Stack: n8n, WhatsApp Business API, Speech-to-Text, Image Analysis, Google Search Tool

⚡ Instant Lead Capture & Notification System

A lightweight, fast-to-deploy automation: the moment a form lead comes in, it triggers an instant auto-reply email, a real-time Slack notification, and a saved record — all in parallel, zero manual steps. Stack: n8n, Webhook Trigger, Gmail API, Slack API, Google Sheets

🎓 Academic Outreach Email Automation

A scheduled pipeline that finds university faculty pages via SerpAPI, then a two-agent AI chain locates faculty directories and extracts professor emails from raw HTML — deduplicated and sent as a formatted outreach email automatically. Stack: n8n, SerpAPI, Two-Agent AI Chain, HTML/Regex Parsing, Gmail API

📱 AI-Generated Instagram Content Pipeline

Runs daily at 9am. Reads the last 12 posted topics so content never repeats, then a three-agent pipeline (strategist → copywriter → image-prompt) builds a ready-to-post content calendar — hook, caption, hashtags, and visual brief. Stack: n8n, Daily Scheduler, 3-Agent Pipeline, Brand Config Memory, Google Sheets

🧠 AI Analysis & Report Generation (Assessment Platform)

A 3-stage AI prompt chain that turns raw assessment data into a boardroom-ready report: Insight Extraction → Recommendations & Strategy → Report Writer/Formatter, with structured JSON output built for direct chart/dashboard rendering. Stack: n8n, OpenAI/Gemini, Structured JSON Output, Multi-Agent Chaining

About Me

I'm an AI Automation & AI Agent Developer specializing in n8n, AI agent orchestration, and API integrations. I build systems that run businesses on autopilot — not proof-of-concepts.

Open to new automation projects — reach out via the contact details above.
